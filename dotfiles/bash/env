# If running non-interactively, don't continue parsing ~/.bashrc
[ -z "$PS1" ] && return

# env vars
export OOO_FORCE_DESKTOP=gnome      # openoffice preferences
export BROWSER="links '%s' &"
export PATH=$PATH:/home/bmartin/.gem/ruby/1.8/bin:~/.bin

# define editor
export EDITOR="nano"

